# Raw camera files — run tools/optimize_images.py on these, commit the output
images/raw/

# Python
__pycache__/

# Source video — keep large originals out of the repo
video/
*.mov
*.mp4
!images/*.mp4
